package com.example.tesis.ui.theme

import androidx.compose.ui.graphics.Color

// Colores de Pronaca (mantener como fallback)
val PronacaRed = Color(0xFFE30613)      // Rojo principal
val PronacaGreen = Color(0xFF006E3D)    // Verde principal
val PronacaDarkGreen = Color(0xFF004D28) // Verde oscuro

// Nueva paleta naranja-rojiza
val PrimaryOrange = Color(0xFFFF6B35)    // Naranja vibrante
val DarkOrange = Color(0xFFE65100)      // Naranja oscuro
val LightOrange = Color(0xFFFFCCBC)     // Naranja claro
val CoralRed = Color(0xFFFF5252)        // Rojo coral
val PinkOrange = Color(0xFFFF8A65)      // Naranja rosado
val WarmRed = Color(0xFFFF5722)         // Rojo cálido
val ConchodeVino = Color(0xFF6B0808)

// Colores neutros
val BackgroundLight = Color(0xFFFFF3E0)  // Naranja muy claro para fondo
val BackgroundWhite = Color(0xFFFFFFFF)  // Blanco
val TextDark = Color(0xFF333333)        // Gris oscuro
val TextGray = Color(0xFF666666)        // Gris medio